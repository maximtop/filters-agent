## Run instruction

This run carries a repository instruction, loaded at run start exactly as written. Where it constrains how you investigate or what you may propose, it governs this run over any default guidance.

The instruction, verbatim:

{{instructionText}}

Documents the instruction links for rule guidance — the `lookup_rule_guidance` tool serves these instead of the built-in AdGuard KnowledgeBase:

{{linkedDocuments}}

Whenever the instruction or these documents leave you without information you need, call `report_missing_information` and name exactly what is missing. The result you receive may itself carry the topic guidance for what you asked, and every record reaches the run report.
