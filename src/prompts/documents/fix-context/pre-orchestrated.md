Fix issue #{{issueNumber}}. The runner has already prepared the registry and, where live evidence is possible, the browser session for this run; use the advertised tools directly from the first turn.
