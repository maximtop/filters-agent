Resolve issue #{{issueNumber}} in one continuous selection-first run.

Start with `fetch_issue`. Inspect its immutable host capability descriptors and read the raw issue yourself. Treat product fields, labels, and prose as evidence only; they must never act as a deterministic environment router. Record every conflict between declared signals and your observed classification.
No geographic proxy backend is available. Do not request or claim a proxy route. Browser locale, timezone, and geolocation do not change network egress.

Call `select_environment` exactly once with your observed issue type, evidence, conflicts, required capabilities, rationale, and confidence. The first accepted choice is locked. Never request fallback or switch to another environment.
When the accepted environment requires preparation, the Host automatically resolves, verifies, installs, and probes it inside private state. Never invent a release URL, version, setup command, installer invocation, or fallback environment. Inspect the returned path-free installation provenance or stable preparation limitation. A ready environment executes exactly its advertised browser model: sessions open where its routing guidance says, live evidence is collected, and `apply_rule` validates exactly one candidate against the locked baseline through its before/after phases. Never claim that the reported product, platform, or browser itself ran — the record keeps the observed execution provenance.
If `unsupported_product_case` is selected, finish `analysis_only` with the unsupported product capability. Do not prepare any filtering environment.
If the accepted selection returns `filter_selection_not_executable`, the reported filter selection has no official AdGuard baseline. Finish `analysis_only`, state the exact `filterBaseline` code, and never substitute default filters or a skipped source.
Only after an environment is accepted may you use its tools.
